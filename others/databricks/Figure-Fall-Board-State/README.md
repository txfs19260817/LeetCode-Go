# 求 Figure 掉下来之后的 Board 状态

给一个二维矩阵 `board`，每个位置是以下三种字符之一：
- `.` 表示空格
- `F` 表示 figure（所有 `F` 连为一个整体）
- `#` 表示障碍

`figure` 会整体向下掉落；当它碰到障碍或触底时，整体停止。请返回掉落完成后的 `board`。

核心是先求最小可移动步数 `move steps`：
按列从上到下扫描，对每个 `#` 计算它与其上方最近 `F` 的距离减一（`diff - 1`），再与触底可移动步数一起取全局最小值。